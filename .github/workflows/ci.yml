name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.0'
          cache: true
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev \
            libasound2-dev \
            pkg-config
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.10.1

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.0'
          cache: true
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev \
            libasound2-dev \
            pkg-config \
            xvfb
      - name: Install Dependencies
        run: go mod download
      - name: Run Tests
        run: xvfb-run go test -v ./...
      - name: Run Benchmarks
        run: xvfb-run go test -bench=. -benchmem ./pkg/bgpengine/...

  build:
    name: Build Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.0'
          cache: true
      - name: Install system dependencies (Linux)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev \
            libasound2-dev \
            pkg-config
      - name: Build Linux
        run: |
          go build -tags=ebitengine ./cmd/bgp-viewer
          go build ./cmd/bgp-data-fetcher
      - name: Build Windows
        run: |
          GOOS=windows GOARCH=amd64 go build -tags=ebitengine ./cmd/bgp-viewer
          GOOS=windows GOARCH=amd64 go build ./cmd/bgp-data-fetcher
      - name: Build macOS
        run: |
          GOOS=darwin GOARCH=amd64 go build ./cmd/bgp-viewer
          GOOS=darwin GOARCH=amd64 go build ./cmd/bgp-data-fetcher
          GOOS=darwin GOARCH=arm64 go build ./cmd/bgp-viewer
          GOOS=darwin GOARCH=arm64 go build ./cmd/bgp-data-fetcher
